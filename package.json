{
  "name": "notebook-jump-telegram",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx wrangler pages dev .",
    "deploy": "npx wrangler pages deploy . --project-name notebook-jump"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}
